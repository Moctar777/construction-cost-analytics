Step 1: Verify the Data Loaded Correctly
SELECT * FROM Projects;
SELECT * FROM Vendors;
SELECT * FROM ActualCosts;
SELECT * FROM BOQ;

Step 2: Count Records
SELECT COUNT(*) FROM Vendors;
Projects
   |
   |
ProjectID
   |
ActualCosts
   |
VendorID
   |
Vendors
Step 4: First Business Question

SELECT SUM(CostAmount) AS TotalSpent
FROM ActualCosts;


Step 5: Cost by Project

SELECT
    ProjectID,
    SUM(CostAmount) AS TotalCost
FROM ActualCosts
GROUP BY ProjectID;


Step 6: Add Project Names
